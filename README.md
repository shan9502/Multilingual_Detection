# Multilingual_Detection
A Flask-based web application that utilizes Groq LLM models to detect and distinguish multiple languages, including English, from text input. The app also includes support for audio and video files.
